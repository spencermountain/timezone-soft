//cool function
const fun = () => {
  let str = ' asdf ';
  str = str.trim();

  const arr = [str];
  if (arr.includes(str)) {
    var a = new Promise();
    return a
  }
};

var tmp = fun;

export default tmp;
